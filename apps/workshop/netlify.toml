[build]
  command = "cd ../.. && pnpm install && pnpm turbo run build --filter=@nrn/workshop"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "11.5.2"

[[plugins]]
  package = "@netlify/plugin-nextjs"
